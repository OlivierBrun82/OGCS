
services:
###> doctrine/doctrine-bundle ###
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    depends_on:
      - database
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      APP_ENV: dev
      DATABASE_URL: "mysql://app:root@database:3306/ogcs?serverVersion=8.0&charset=utf8"
    # Serveur PHP : si public/assets existe (suite à asset-map:compile), php -S sert ces
    # fichiers en BYPASS Symfony → ancien CSS figé et souvent Content-Type: text/html sur .css :
    # le navigateur n’applique pas les changements ni les correctifs (ex. menu burger).
    # Au démarrage on vide public/assets pour que /assets/* soit toujours géré par Symfony.
    command: sh -c "rm -rf public/assets && exec php -S 0.0.0.0:8000 -t public public/index.php"
    ports:
      # hôte:conteneur — le serveur intégré écoute sur 8000 dans l’image
      - "8005:8000"
  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ${MYSQL_DATABASE:-ogcs}
      MYSQL_USER: ${MYSQL_USER:-app}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-root}
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 5s
      retries: 5
      start_period: 60s
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    depends_on:
      - database
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: root
    # Port hôte configurable (évite les conflits avec 8080). Ex. dans .env : PHPMYADMIN_PORT=8090
    ports:
      - "${PHPMYADMIN_PORT:-8081}:80"
###< doctrine/doctrine-bundle ###

volumes:
###> doctrine/doctrine-bundle ###
  mysql_data:
###< doctrine/doctrine-bundle ###
