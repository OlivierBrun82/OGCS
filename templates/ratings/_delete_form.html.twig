<form method="post" action="{{ path('players_ratings_delete', {id: rating.id}) }}" class="d-inline" onsubmit="return confirm('Supprimer définitivement cette note ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rating.id) }}">
    <button class="btn btn-outline-danger btn-sm">Supprimer</button>
</form>
